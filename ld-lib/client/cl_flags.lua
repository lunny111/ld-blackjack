function SetPedFlag(object, flag, enabled)
    exports['tp-flags']:SetPedFlag(object, flag, enabled)
end

function HasPedFlag(object, flag)
    return exports['tp-flags']:HasPedFlag(object, flag)
end

function GetPedFlags(object)
    return exports['tp-flags']:GetPedFlags(object)
end

function SetPedFlags(object, flag)
    exports['tp-flags']:SetPedFlags(object, flag)
end

function SetVehicleFlag(vehicle, flag, enabled)
    exports['tp-flags']:SetVehicleFlag(vehicle, flag, enabled)
end

function HasVehicleFlag(vehicle, flag)
    return exports['tp-flags']:HasVehicleFlag(vehicle, flag)
end

function GetVehicleFlags(vehicle)
    return exports['tp-flags']:GetVehicleFlags(vehicle)
end

function SetVehicleFlags(object, flag)
    exports['tp-flags']:SetVehicleFlags(object, flag)
end

function SetObjectFlag(object, flag, enabled)
    exports['tp-flags']:SetObjectFlag(object, flag, enabled)
end

function HasObjectFlag(object, flag)
    return exports['tp-flags']:HasObjectFlag(object, flag)
end

function GetObjectFlags(object)
    return exports['tp-flags']:GetObjectFlags(object)
end

function SetObjectFlags(object, flag)
    exports['tp-flags']:SetObjectFlags(object, flag)
end